<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.workout.paramp.theworkout.AdminActivity">

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Focus Area"
        android:id="@+id/admin_focus_area"
        android:onClick="on_admin_focus_area_click"/>
    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Exercise"
        android:id="@+id/admin_exercise"
        android:layout_below="@+id/admin_focus_area"
        android:onClick="on_admin_exercise_click"/>
    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Workout"
        android:id="@+id/admin_workout"
        android:layout_below="@+id/admin_exercise"
        android:onClick="on_admin_workout_click"/>
    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Restore Database"
        android:id="@+id/admin_restore_db"
        android:layout_below="@+id/admin_workout"
        android:onClick="on_admin_restore_db_click"/>
</RelativeLayout>
